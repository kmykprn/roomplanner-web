# Firebase のウェブAPIキー。
#
# 秘密ではない（ビルド後のJSに必ず入る）が、リポジトリには置かない。
# 理由は src/config/api.ts のコメントを参照。
#
# ローカルで生成を試すときは、このファイルを .env.local に写して値を入れる。
# .env.local は gitignore してある。
#
# 値の取得方法は Hunyuan3D-2GP の infra/HANDOFF.md にある。
VITE_FIREBASE_API_KEY=
