# ディレクトリだけでなくシンボリックリンクも除く。
# worktree で node_modules へのリンクを置いたとき、`node_modules/` だけでは
# リンク（ファイル扱い）が一致せずコミットされてしまった
node_modules/
node_modules
dist/
.DS_Store
*.local
.vscode/
# Capacitor（step 4 以降で追加される）
ios/
android/

# 実際の値が入る。.env.example だけを追跡する
.env
.env.local
