# roomplanner-web

Vite + TypeScript + three.js による 3D 部屋模様替えアプリ。
[roomplanner-v4](https://github.com/kmykprn/roomplanner-v4)（Expo + React Native + React Three Fiber）を、
React を使わずに書き直したもの。

React の宣言的レンダリングを 3D シーングラフに被せることをやめ、
「触りたいオブジェクトを直接触る」書き方に戻すのがこのリポジトリの主旨。
新しいコードもこの方針に沿わせること。

# 開発ルール

以下のルールは毎回復唱し、必ず守ること：

- **実装する前には必ず確認を取ること**
- **バグ修正でも確認を取ること**
- **変更後はpushすること**
- **新規作業やリファクタリングは新しいブランチで作業し、mainへPRを出すこと**
- **PR前には最新のmainをpullして、コンフリクトがない状態でpushすること**
- **実装の許可が得られたら、コミットとPR作成は許可不要**
- **2回実装してバグが解決されない場合、エラーログを入れて問題を正確に調査すること**
- **ログを追加してデバッグしても問題が解決できない場合、ユーザにウェブ調査してほしいという旨を返すこと**

# PR作成時のルール

- **PRを出すときは、変更内容を必ず `pr-logic-map` スキルの形式でアーティファクトに落とし、PRの説明にURLを貼ること**
  - 1メソッドあたり数行の日本語でロジックを要約し、コードは折りたたんで隠す形式
  - 目的はレビュー負荷の削減。レビュアーはまず日本語だけを追い、疑わしい箇所でコードを開く
  - 詳細は `.claude/skills/pr-logic-map/SKILL.md` を参照
- **修正内容は、可能な限りヘッドレスブラウザで動作を確認してから出すこと**
  - Chromium が `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` にある
  - バグ修正では「直ったこと」と「デグレしていないこと」の両方を確認する

# レビュールール

1. **使われていない変数や、不要なコードがないか確認すること**
2. **変数名が分かりやすいかを確認すること**
3. **共通的な処理は関数化されていること**
4. **複雑な処理には、コメントがつけられており、何を目的としたコードかが分かるようになっていること**

レビュー完了後、問題がないものはmainブランチにマージする。

# カラーテーマ仕様

アプリ全体で統一されたカラーテーマを使用すること（定義は `src/config/theme.ts`）：

- **メインカラー（青）**: #0a7ea4
- **テキストカラー（濃いグレー）**: #11181C
- **背景色**: #f5f5f5

# 部屋の3D座標系仕様

定義は `src/config/room.ts` に集約してある。壁の位置・回転・法線を知る必要があるときは
`getWallTransform()` を参照し、値を各所に書き写さないこと。

## 座標系の定義

- **原点（0, 0, 0）**: 部屋の中央の床
- **X軸**: 右が正、左が負
- **Y軸**: 上が正、下が負（床が0）
- **Z軸**: 手前が正、奥が負

## 部屋のサイズ

- **デフォルトサイズ**: 幅6m × 奥行き4m × 高さ2.5m

## 壁の位置と向き

| 壁 | 位置 | Y軸回転 | 内側を向く法線 |
|---|---|---|---|
| north（奥） | [0, h/2, -d/2] | 0 | [0, 0, 1] |
| south（手前） | [0, h/2, d/2] | π | [0, 0, -1] |
| west（左） | [-w/2, h/2, 0] | π/2 | [1, 0, 0] |
| east（右） | [w/2, h/2, 0] | -π/2 | [-1, 0, 0] |

## 底面基準の統一ルール

すべての3Dオブジェクトは**底面基準**で作ること。
高さ h のオブジェクトは、メッシュを `y = h/2` に置き、グループの原点が底面に来るようにする。

```javascript
// 正しい実装例（底面基準）
mesh.position.y = height / 2;
```

こうすると配置時の `position.y` が「床からの高さ」そのものになる。

- Y=0：床に直接配置
- Y=0.8：床から80cm上に配置

# 操作の扱い

- **家具の移動は1本目の指だけで行う。2本目以降はカメラ操作に渡すこと**
  - `event.isPrimary` で判定する。この判定が抜けると、家具を掴んだまま2本目の指を置いたとき、
    カメラ操作のつもりの動きで家具が動いてしまう
- タップとドラッグの区別は移動距離で行う（8px未満ならタップ）

# アーキテクチャの方針

## 自前構築を既定とする

- **外部APIに依存せず、自前で構築することを基本方針とする**
  - 理由は長期的な管理コスト。他社APIは値上げ・仕様変更・廃止・サービス終了が
    こちらの都合と無関係に起きるため、動いていたものが突然壊れる
  - 実例: RunPodのエンドポイントは知らぬ間に削除されていた。
    Tripo は V2 API を 2026年11月に停止する。
    旧実装は Synexa という外部APIに依存していたが、コードを読むまで気づけなかった
- **コストで外部APIが有利でも、原則として自前を選ぶ**
  - 判断材料としてコスト比較は提示するが、既定は自前
  - 外部APIを選ぶのは、明示的にそう判断した場合のみ
- **検証目的で一時的に外部APIを使う場合も、必ず差し替え可能な形にする**
  - 自前のAPI層（プロキシ）の背後に置き、呼び出し側が特定のベンダーを知らない状態を保つ
  - APIキーは絶対にクライアントに露出させない（公開リポジトリ・公開サイトのため）

## 3Dモデル生成

- **Hunyuan3D-2GP を自前で動かす**（https://github.com/kmykprn/Hunyuan3D-2GP）
  - 動作するCLIと非同期APIサーバ（`POST /send` → `GET /status/{uid}`）が実装済み
  - 既定モデル: 形状 `Hunyuan3D-2mini` + テクスチャ `Hunyuan3D-2`
  - mmgp の profile はVRAMと速度のトレードオフ。秒課金の環境では
    オフロードを減らす（profile番号を小さくする）ほうが速く・安くなる
  - テクスチャ生成にはCUDA拡張のビルドが必要
    （`custom_rasterizer` / `differentiable_renderer`）

# ネイティブ依存の扱い

将来 Capacitor で iOS アプリにする予定があるため、以下を守ること。

- **OS依存の処理（カメラ・写真ライブラリ・ファイル保存）は `src/platform/` 経由でのみ呼ぶこと**
  - 差し替えが1ファイルで済むようにするため
- **オフラインで起動できる状態を保つこと**
  - 起動時にサーバーを必要とする設計にしない
  - ネット断で白画面になるWebViewアプリは App Store のガイドライン4.2で弾かれる
- **ベースパスの既定は `./`（相対パス）**
  - GitHub Pages へのデプロイ時のみ `DEPLOY_BASE` で上書きする
